# dheenaxe's Nix Config

# Setup Instructions

```
git clone https://github.com/dheenaxe/nix-config dheenaxe-nix-config
```

```
ln -fs "$PWD/system/configuration.nix" "/etc/nixos/configuration.nix"
sudo nixos-rebuild switch
```
